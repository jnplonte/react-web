import { makeStyles } from '@mui/styles';

export const userTableStyles = makeStyles((theme: any) => ({
	root: {
		overflowX: 'auto',
	},
	table: {
		minWidth: 1050,
	},
	dialog: {},
	modal: {},
}));
