import { makeStyles } from '@material-ui/styles';

export const sidebarStyle = makeStyles((theme: any) => ({
    root: {},
    item: {
      display: 'flex',
      paddingTop: 0,
      paddingBottom: 0,
    },
}));
